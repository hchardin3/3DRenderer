#include "display.hpp"
#include <iostream>

void Display::displayRenderAsImage(const Scene& scene) {
    Render render = scene.getRender();


    std::cout << "Displaying render as image" << std::endl;
}